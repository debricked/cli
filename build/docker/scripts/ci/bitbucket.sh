#!/usr/bin/env bash

# shellcheck disable=SC2034

DEBRICKED_SCAN_REPOSITORY="${BITBUCKET_REPO_OWNER}/${BITBUCKET_REPO_SLUG}"
DEBRICKED_SCAN_COMMIT="${BITBUCKET_COMMIT}"
DEBRICKED_SCAN_BRANCH="${BITBUCKET_BRANCH}"
DEBRICKED_SCAN_REPOSITORY_URL="${BITBUCKET_GIT_HTTP_ORIGIN}"
DEBRICKED_SCAN_INTEGRATION=bitbucket
DEBRICKED_SCAN_PATH="."
if command -v git &> /dev/null
then
  DEBRICKED_SCAN_AUTHOR="$(git log -1 --pretty=%ae)"
fi